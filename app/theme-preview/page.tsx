import { ThemePreview } from "@/components/ui/theme-preview";

export const metadata = {
  title: "Theme Preview | RUDRA Design System",
  description: "RUDRA Superhero Brand Token System Preview Page",
};

export default function ThemePreviewPage() {
  return <ThemePreview />;
}
